---
packages:
  - akmod-nvidia
  - xorg-x11-drv-nvidia-cuda

post_install:
  run:
   # - sudo grubby --args="iommu=pt" --update-kernel=ALL
   # - ln -s /sys/class/hwmon/hwmon7/temp1_input ~/.local/share/cpu_temp
